# npm 10 crashes resolving the optional vite devtools peers pulled in by vitest 4
# (arborist "edgesOut" error). The peers this package needs (@vitest/runner, @vitest/snapshot)
# arrive as vitest dependencies.
legacy-peer-deps=true
